/*
 * POSMall Laravel Mix storefront entry.
 *
 * Import storefront JavaScript here when optimized assets are enabled.
 * If a dynamic feature depends on a selector/class, keep the selector in this entry or in
 * webpack.mix.js PurgeCSS safelist before running `npm run prod`.
 */
import '../../posmall/js/storefront';
